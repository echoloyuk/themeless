<div class="index-nav">
    {{#foreach navigation}}
    <a href="{{url absolute="true"}}" class="nav-item nav-{{slug}}{{#if current}} nav-current{{/if}}">{{label}}</a>
    {{/foreach}}
    <a class="subscribe-button icon-feed" href="{{@blog.url}}/rss/">RSS</a>
</div>
<span class="nav-cover"></span>
